import Blogs from "@/pages/blogs/components/blogs";

const BlogsView = () => {
  return (
    <div className="mt-9 flex min-h-screen flex-col items-center">
      <Blogs />
    </div>
  );
};

export default BlogsView;
